# --- Database Connection ---
# Connects to your Docker MySQL container on port 3307
# Uses a SEPARATE database name 'pos_system_test' to protect your real data
DATABASE_URL="mysql://pos:pos%40123@localhost:3307/pos_system_test"

# --- JWT Secret ---
# Uses a simple secret for testing purposes
JWT_SECRET="aaqooiwamloppqawmnlkaqzlposwanslifwadftwksaamfjalas"

# --- API Keys ---
# A fake key is fine because we will mock the AI service in tests
GOOGLE_API_KEY="fake_google_api_key_for_testing"